

Point Class 
	init(list)
		list (point 1, point 2, point 3,,,,,,)

Line Segment Class (subset of point class) 
	init(list, list)
		Head point (list)
		End point (list)
	
Circle Class (subset of point class)
	center point
	Radius 
	
space Class 
	init(name, dimention)
		name of the space
		dimension of the space
		list of points
		list of lines
		list of circles
	
		add_point function()
		add_line function()
		add_circle function()
		display_all_entities function()

Main()
	create a list for spaces
	call load_data function()
	
	while ( true )
		Ask user if they want to use already created plane or create new plane
		If (use already created plane)
			Show a list of planes and let user select a plane
		Else
			Let user create new plane with dimension (name of the plane and dimension)

		Get list/array of points, line, and circle for the plane
		
		while (true) 
			ask user
			(
				1. Create point
				2. Create line
				3. Create circle
				4. Line segment intersection
				5. Closest pair of points
				6. Convex hull
				7. Largest empty circle
				8. Display all entities
				9. (possibly) Change working plane
				10. exit
			)

			If (1)
				call add_point function()
			If(2)
				call add_line function()
			If(3)
				call add_radius function()
			If(4)
				call Line_Segment_Intersection function()
			If(5)
				call Closest_Pair_of_Points function()
			If(6)
				call Convex_Hull function()
			If(7)
				Largest_empty_circle function()
			If(8)
				call display_all_entities function()
			If(9)
				break the while loop
			If(10)
				Exit the program

load_data function()
	load a file for spaces, create space, add them to the list 

Line_Segment_Intersection function()
	

Closest_Pair_of_Points function()


Convex_Hull function()


Largest_empty_circle function()




